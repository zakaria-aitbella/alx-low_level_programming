#include "main.h"
/**
 * is_prime_number - returns 1 if the input integer is a prime number,
 * otherwise return 0.
 * @n: int
 * Return: int
*/
int (is_prime_number(int n)
{

if (n / n = n && n * 1 = n)
{
return (1);
}
else
{
return (0);
}
return (n);
}
